spring:
  application:
    name: demo

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/database0?serverTimezone=Asia/Seoul
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

jwt:
  expiration_time: 1
  secret : DemoKeyHasToBeOver32ByteABCDEFGHIJKLMNOPQRSTUVWXYZ